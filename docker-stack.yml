version: '3.9'

networks:
  nutria_overlay:
    driver: overlay

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: nutria
      POSTGRES_PASSWORD: nutria_pass
      POSTGRES_DB: nutria
    volumes:
      - nutria_pg_data:/var/lib/postgresql/data
    networks:
      - nutria_overlay

  redis:
    image: redis:7-alpine
    networks:
      - nutria_overlay

  rabbitmq:
    image: rabbitmq:3.12-management
    environment:
      RABBITMQ_DEFAULT_USER: nutria
      RABBITMQ_DEFAULT_PASS: nutria
    ports:
      - "15672:15672"
    networks:
      - nutria_overlay

  minio:
    image: minio/minio:latest
    command: server /data
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
    volumes:
      - minio_data:/data
    networks:
      - nutria_overlay

  api:
    image: yourdockerhubusername/nutria-backend:latest
    environment:
      DATABASE_URL: postgresql://nutria:nutria_pass@postgres:5432/nutria
      RABBITMQ_HOST: rabbitmq
      REDIS_HOST: redis
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      JWT_SECRET_KEY: supersecretkey
      OPENAI_API_KEY: "${OPENAI_API_KEY}"
    depends_on:
      - postgres
      - redis
      - rabbitmq
      - minio
    networks:
      - nutria_overlay
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
      resources:
        limits:
          cpus: '0.5'
          memory: 512M

  worker_suporte_nutri:
    image: yourdockerhubusername/nutria-backend:latest
    command: ["python", "-m", "app.workers.suporte_nutri_worker"]
    environment:
      DATABASE_URL: postgresql://nutria:nutria_pass@postgres:5432/nutria
      RABBITMQ_HOST: rabbitmq
      REDIS_HOST: redis
      MINIO_ENDPOINT: minio:9000
    depends_on:
      - postgres
      - redis
      - rabbitmq
      - minio
    networks:
      - nutria_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  worker_agente_lembretes:
    image: yourdockerhubusername/nutria-backend:latest
    command: ["python", "-m", "app.workers.agente_lembretes_agendamento_worker"]
    environment:
      DATABASE_URL: postgresql://nutria:nutria_pass@postgres:5432/nutria
      RABBITMQ_HOST: rabbitmq
      REDIS_HOST: redis
    depends_on:
      - postgres
      - rabbitmq
      - redis
    networks:
      - nutria_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  worker_atualizar_agendamento:
    image: yourdockerhubusername/nutria-backend:latest
    command: ["python", "-m", "app.workers.atualizar_agendamento_worker"]
    environment:
      DATABASE_URL: postgresql://nutria:nutria_pass@postgres:5432/nutria
      RABBITMQ_HOST: rabbitmq
    depends_on:
      - postgres
      - rabbitmq
    networks:
      - nutria_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  worker_baixar_enviar_arquivo:
    image: yourdockerhubusername/nutria-backend:latest
    command: ["python", "-m", "app.workers.baixar_enviar_arquivo_worker"]
    environment:
      DATABASE_URL: postgresql://nutria:nutria_pass@postgres:5432/nutria
      RABBITMQ_HOST: rabbitmq
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    depends_on:
      - postgres
      - rabbitmq
      - minio
    networks:
      - nutria_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  worker_buscar_criar_contato_conversa:
    image: yourdockerhubusername/nutria-backend:latest
    command: ["python", "-m", "app.workers.buscar_criar_contato_conversa_worker"]
    environment:
      DATABASE_URL: postgresql://nutria:nutria_pass@postgres:5432/nutria
      RABBITMQ_HOST: rabbitmq
      REDIS_HOST: redis
    depends_on:
      - postgres
      - rabbitmq
      - redis
    networks:
      - nutria_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  worker_desmarcar_agendamento:
    image: yourdockerhubusername/nutria-backend:latest
    command: ["python", "-m", "app.workers.desmarcar_agendamento_worker"]
    environment:
      DATABASE_URL: postgresql://nutria:nutria_pass@postgres:5432/nutria
      RABBITMQ_HOST: rabbitmq
    depends_on:
      - postgres
      - rabbitmq
    networks:
      - nutria_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  worker_gestao_ligacoes:
    image: yourdockerhubusername/nutria-backend:latest
    command: ["python", "-m", "app.workers.gestao_ligacoes_worker"]
    environment:
      DATABASE_URL: postgresql://nutria:nutria_pass@postgres:5432/nutria
      RABBITMQ_HOST: rabbitmq
      REDIS_HOST: redis
    depends_on:
      - postgres
      - rabbitmq
      - redis
    networks:
      - nutria_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  worker_recuperacao_leads:
    image: yourdockerhubusername/nutria-backend:latest
    command: ["python", "-m", "app.workers.recuperacao_leads_worker"]
    environment:
      DATABASE_URL: postgresql://nutria:nutria_pass@postgres:5432/nutria
      RABBITMQ_HOST: rabbitmq
      REDIS_HOST: redis
    depends_on:
      - postgres
      - rabbitmq
      - redis
    networks:
      - nutria_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

  worker_retell_secretaria_v3:
    image: yourdockerhubusername/nutria-backend:latest
    command: ["python", "-m", "app.workers.retell_secretaria_v3_worker"]
    environment:
      DATABASE_URL: postgresql://nutria:nutria_pass@postgres:5432/nutria
      RABBITMQ_HOST: rabbitmq
      REDIS_HOST: redis
    depends_on:
      - postgres
      - rabbitmq
      - redis
    networks:
      - nutria_overlay
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure

volumes:
  nutria_pg_data:
  minio_data:

